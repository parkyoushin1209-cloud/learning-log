모니터(커버리지, 스코어보드, 검사를 가짐)
uvm_analysis_imp - 다른 모니터로부터 정보를 받음
uvm_analysis_port - 스코어보드 같은 외부 컴포넌트로 정보 전달
스코어보드, 커버리지, 설정 객체, 가상 인터페이스
write 구현

빌드페이즈
스코어보드, 커버리지 생성
설정객체가 없으면 생성 후 데이터베이스에 등록

커넥트페이즈
스코어보드와 연결해주기
가상 인터페이스 얻기



env
모니터, 레지스터 모델, 프레딕터, 어댑터, 설정 객체
port, imp

write 구현

빌드페이즈
모니터 생성, 어댑터 생성, 프레딕터 생성
설정 객체 없으면 생성 후 등록
레지스터 모델 없으면 error 띄우기

커넥트 페이즈
프레딕터의 맵, 어댑터 할당


설정 객체
캡슐화 해 두기
제약, rand knob들을 가짐
버스 트랜잭션으로부터 설정됨

설정 모드
화이트박스/블랙박스
화이트 박스 : DUT 내부 구현을 알고 있음, 더욱 정확하고 특정적인 검증 가능
블랙 박스 : 내부 구현을 모름, 입력에 대한 결과만을 알 뿐-> DUT가 바뀌어도 재사용 가능함

액티브/패시브
액티브 모드 : 대행모드를 구현(DUT가 없어도 DUT의 기능을 함)
DUT를 대신해 외부 인터페이스로 구동에 추가적인 가상 시퀀서 사용을 가능하게 함

추상화 수준 놉을 사용한 추상화 수준 변경

재설정을 위해 설정 메커니즘 사용은 비적절
객체 획득 시 참조를 얻으므로 가시성이 생겨버림
각 에이전트에 대해 UVM_ACTIVE_PASSIVE_ENUM 할당
UVC는 env 1개를 의미

UVC를 통합한 테스트벤치의 설정 객체에서 하위 UVC의 설정 객체 인스턴스화
