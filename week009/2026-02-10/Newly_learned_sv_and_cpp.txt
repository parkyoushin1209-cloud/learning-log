c++
파생된 클래스에서 지켜져야 하는 규칙이 있는 경우 관련 함수는 protected에 두며
기반 클래스로 사용할 클래스의 생성자도 protected에 직접 사용하지 못하게 하기

이렇게 기반 클래스로만 사용 가능한 클래스를 추상클래스라고 함
추상클래스를 정의하는 더 일반적인 방법은 순수 가상 함수
객체 생성이 가능한 클래스는 실체 클래스

기반 클래스가 아닌 파생 클래스에서 관련된 함수(draw_lines() 나 move()의 경우 각 도형들이 자신을 어떻게 표현해야 하는지, 어떤 점을 포함하는 지등에 대한 정보를 알 수 있음)에 대한 정보를 관리하는 경우
기반 클래스에서 virtual로 사용하기


sv

| 기준                 | 방법 1 (interface clk 없음)                          | 방법 2 (interface clk 포함)           |
| ----------------    | ------------------------------ -------------------   | -------------------------------------  |
| 재사용성           | 높음, 다른 clk domain DUT에도 재사용 가능 | 낮음, clk domain 고정               |
| 캡슐화              | 낮음, clk 처리 별도로 해야 함                     | 높음, interface 안에서 처리 가능  |
| multi-clock test  | 유연, 자유롭게 phase/주파수 조절 가능        | 제한적, interface 안 clk 사용       |
| TB 코드             | 약간 길어짐, clk 토글 task 필요                   | 짧음, clk 바로 사용 가능            |
 
클럭 발생기 모듈을 따로 설정하고 하기 탑에서 모듈 파생 및 인터페이스에 여기서 만들어지는 클럭 전송하기
아웃풋으로 input 에 assign 불가 => 클럭 발생기에서 output인자로 받기

드라이브, 모니터 run => 송수신 및 콜백 적용 (forever begin end)
send/receive에서 mem을 일정 크기로 나눠서 보낼 경우 (forever begin end 사용)

drv, mon, 등등 내부 함수는 virtual 아닌 경우가 보통임
스코어보드 -> save/check/new/wrap_up

커버리지 샘플링
=> 커버리지 콜백 클래스에 커버그룹 두고
post_tx에서 sample

test의 빌드 런 사이에서 new하고 해당 콜백 큐에
커버클래스에서 sample 함수 만들기 인자리스트로 받은 후 (this.x = x)방식 이용 sample은 랜덤화가 아니고 현재 값 기준임 
