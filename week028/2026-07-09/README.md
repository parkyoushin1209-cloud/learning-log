191일차 학습한 내용
1. SVA 가이드라인
naming convention
특성을 나타내는 식별자_속성.확장자 형식을 사용하기
(속성 : _tb, _rtl, _pkg, _pgm, _cb, _mp 등등)
어써션 지시어에 대해 (a_, c_, m_, r_ 사용)
변수에는 v_, 지역 변수에는 lv_
endmodule, endpackage 등 뒤에 : 식별자 붙여주기
상수는 대문자로 

선언 가능 장소 & 지시어 가능 장소
합성 가능한 RTL : 화이트박스 검증을 목표로 설계자가 주로 사용, 이후 수정 시 불필요한 재합성을 해야 할 수도 있음
체커 : 관련된 어써션을 두고 바인드 가능함, 모듈/인터페이스 등으로 바인드 가능하며, 프로시저 블록에서도 인스턴스화 가능함, 합성 툴에 의해 무시됨
모듈 : 체커가 없을 때 대안, 모듈이나 프로그램에서만 인스턴스화 가능함
인터페이스 : 인터페이스 변수들과 깊게 묶임
패키지 : 지시어는 올 수 없으나 체커를 포함한 후 import 해 사용 가능함
TB

let 생성자 : 지역적임, 가독성을 높이기 위해 사용 가능함

enabling condition : 모든 조건이 만족된 후 프로퍼티가 평가되도록 하기


선정의 VS 인라인
지역 변수를 가진 어써션의 경우 선정의 할 필요가 있었음, 선정의 된 경우 재사용 가능하나 선언부와 어써션 사용 시 코드의 길이가 증가함
먼저 정의할 경우 명명된 시퀀스/프로퍼티를 사용해 가독성을 높일 수 있음  
파라미터는 시퀀스/프로퍼티에 둘 수 없음 -> 형식 인자 이용 가능

반드시 종료되어야 함을 나타낼 때는 strong 이용

clocking 선언법
1. 전역 클로킹($global clock) 사용
2. 디폴트 클럭킹 사용(디폴트 클럭킹 선언시 전역 클로킹으로의 참조를 가질 수 있으며 이는 모듈에서만 가능)
3. 어써션 내부에 명시적으로 클럭킹을 두기
4. 멀티 클럭을 가질 경우 클럭을 명시적으로 두어 명확하게

조합 논리(always_comb) 에서는 즉각적 지연된 어써션 #0과 final 지연된 어써션을 사용 가능함

abort 방식
1. disable iff : 일반적인 경우 사용, 아무런 결과도 생기지 않음
2. accept_on, reject_on : 동기화/비동기화 버전 존재, 공허한 성공 또는 실패로 이어짐, 중첩될 수 있음, 프로토콜 특정 강제종료 명시할 때 사용

다이나믹 배열, 큐, 연관 배열 등은 어써션에서 사용될 수 았음 지원되지 않을 경우, 지역 변수에 복사해서 사용하기
cyclic dependency 시퀀스가 서로를 참조할 경우 순환 의존성이 생기며 에러가 발생함


어써션 고유성 보장
지연 범위, 반복 범위, and/or.intersect + [=n]을 가진 경우 선행조건의 고유성을 보장해줘야 함
멀티 스레드가 선행조건에서 생길 수 있는 경우 first_match 사용

조건이 같다면 |->, |=> 등 사용 -> 트리거링 여부도 알 수 있음
meta-logical value : x,z 값의 부정도 x, z이므로 주의해서 사용
3상태 구동 : inout이어야 함, 더 큰 환경으로 통합될 경우 새로운 구동자가 생길 수 있으므로 주의하기(디버깅을 위해 하나의 구동자만 가지는 지역 변수를 사용 가능)

피해야할 프로퍼티 
공허한 프로퍼티 피하기
모순적인 프로퍼티 작성 피하기

연산 주의 사항 : +1'b1 과 +1은 다른 결과를 초래할 수 있음

모듈/체커 변수 수정 법
1. 시퀀스 매치 아이템에서 함수 호출
2. 액션 블록에서 사용 // 이 경우 실패하거나 성공한 경우에만 업데이트 됨

first_match 없이 [=n] 사용 x



어써션/프로퍼티 작성 과정
1. 디자인 특성 식별
2. 복잡한 어써션 파티션(명명된 시퀀스 사용)
3. 가독성 향상을 위해 end point 사용
4. 체커나 모듈을 사용해 재사용 가능한 검증 유닛 라이브러리 정의
(공통으로 사용하는 어써션, 검증 IP를 포함함)

어써션 디버깅 방법
1. 시뮬레이터/형식 검증 툴 등의 디버깅 능력 이해
2. 메시지나 신호의 값을 액션 블록에 추가($sampled 사용 가능)
3. 가정을 유효화하기 위해 제약 사용
4. 기능을 묘사하는 이름 사용
5. 다른 사람도 쉽게 이해할 수 있도록 작성
