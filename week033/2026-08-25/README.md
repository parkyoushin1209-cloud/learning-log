233일차 학습한 내용
1. FIFO 인스턴스 및 포트 연결 작업
2. 송수신 로직 생성(1)
3. 일본어 학습


이번에 async FIFO를 배운 후 RTL을 여러 모듈로 쪼개는 방식으로 설계하는 방식으로 전환했으며 이를 통해 각 모듈의 역할을 분리하면서 모듈화의 중요성을 느낄 수 있었던
것 같다. 특히, 단일 모듈만 만들 때는 너무 복잡하게 돼버려 어떤 신호들을 연결해줘야 할 지 모르겠는 모호함으로 인해 어려움을 겪었으나 모듈을 별개로 분리해서 구현하다
보니 이러한 문제들도 많이 해소된 것 같다. 추가로 이전에는 시프트 레지스터와 FIFO간 경계가 불분명하다는 느낌이었으나 이번에 수정하면서 두 하드웨어 사이의 분리를 확실히
할 수 있었다.
